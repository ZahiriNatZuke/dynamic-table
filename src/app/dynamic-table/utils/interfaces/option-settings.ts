export interface OptionSettings {
    iconNew: string,
    Update: IconSettings,
    Delete: IconSettings
}

export interface IconSettings {
    icon: string,
    literal: string,
    color: string
}
