export interface OptionSettings {
    iconNew: string,
    eventNew: string,
    options: ButtonSettings[]
}

export interface ButtonSettings {
    icon: string,
    literal: string,
    color: string,
    event: string
}
